---
import Layout from "@/layout/Layout.astro";
import Footer from "@/components/Footer.astro";
import { useTranslations } from "@/utils/i18n";
import { X } from "@lucide/astro";
const lang = Astro.props.lang ?? "zh";
const i18n = useTranslations(lang);
const { info } = i18n;
const { sections } = info;
const academiaSinicaHref = lang === "en" ? "/2026/en/info/academia-sinica/" : "/2026/info/academia-sinica/";
---

<Layout title={lang === "en" ? "Event Info - SITCON 2026" : "活動資訊 - SITCON 2026"} lang={lang}>
	<header>
		<div class="hero-content">
			<h1 class="hero-title">{info.mainHeading}</h1>
			<p set:html={info.mainDetails.join("<br />")} />
			<button class="hero-button" data-open-transport>{info.mainButton}</button>
		</div>

		<button class="hero-into-button" id="into-academia-sinica" data-academia-sinica-href={academiaSinicaHref}>{info.intoAcademiaSinica}</button>
		<div class="hero-bg" aria-hidden="true">
			<img src="/2026/img/academia-sinica.svg" alt="" class="hero-bg-img" />
		</div>
	</header>

	<main>
		<div class="activities-container">
			<h2 class="text-reveal-title">{info.activitiesHeading}</h2>
			{
				sections?.map((section: { title: string; activities?: { title: string; body?: string; image: { url: string; alt?: string; position?: string } }[] }) => (
					<div class="activities-section">
						<h3 class="text-reveal-title">{section.title}</h3>

						{section.activities?.map((activity: { title: string; body?: string; image: { url: string; alt?: string; position?: string } }) => {
							return (
								<div class="activity">
									<img class="activity-image" src={activity.image.url} alt={activity.image.alt ?? activity.title} loading="lazy" />
									<div>
										<h4 class="text-reveal-title">{activity.title}</h4>
										{activity.body
											?.split("\n\n")
											.map((paragraph: string) => paragraph.trim())
											.filter(Boolean)
											.map((paragraph: string) =>
												paragraph.trimStart().startsWith("<table") ? <div class="text-reveal-paragraph" set:html={paragraph} /> : <p class="text-reveal-paragraph" set:html={paragraph} />
											)}
									</div>
								</div>
							);
						})}
					</div>
				))
			}
		</div>

		<div class="souvenirs-container">
			<h2 class="text-reveal-title">{info.souvenirs.title}</h2>
			<div class="souvenirs-grid">
				{
					info.souvenirs.items.map((item: { name: string; price: string; image: string }) => (
						<div class="souvenir-card">
							<img class="souvenir-image" src={item.image} alt={item.name} loading="lazy" />
							<div class="souvenir-info">
								<h3 class="souvenir-name">{item.name}</h3>
								<p class="souvenir-price">{item.price}</p>
							</div>
						</div>
					))
				}
			</div>
		</div>
	</main>

	<dialog class="modal" data-transport-dialog>
		<div class="modal-card" data-lenis-prevent>
			<div class="modal-header">
				<h2 class="modal-title">{info.transport.title}</h2>
				<button class="modal-x" type="button" data-close-transport aria-label={lang === "en" ? "Close transportation dialog" : "關閉交通資訊"}>
					<X aria-hidden="true" />
				</button>
			</div>

			<div class="modal-body" data-lenis-prevent>
				<div class="modal-button-container" role="tablist" aria-label={info.transport.title}>
					<button class="modal-button is-active" type="button" data-transport-tab="shuttle" role="tab" aria-selected="true">{info.transport.tabs.shuttle}</button>

					<button class="modal-button" type="button" data-transport-tab="public" role="tab" aria-selected="false">{info.transport.tabs.public}</button>

					<button class="modal-button" type="button" data-transport-tab="drive" role="tab" aria-selected="false">{info.transport.tabs.drive}</button>
				</div>

				<div class="transport-panel-viewport" data-lenis-prevent>
					<section class="transport-panel" data-transport-panel="shuttle" role="tabpanel">
						<div class="transport-route">
							<div class="transport-route-left">{info.transport.shuttle.from}</div>

							<div class="transport-route-line" aria-hidden="true"></div>

							<div class="transport-route-right">{info.transport.shuttle.to}</div>
						</div>

						<div class="transport-card">
							<p>{info.transport.shuttle.location}</p>
							<p>{info.transport.shuttle.time}</p>
							<p>{info.transport.shuttle.interval}</p>
						</div>
					</section>

					<section class="transport-panel" data-transport-panel="public" role="tabpanel" hidden>
						<div class="transport-section">
							<h3 class="transport-section-title">{info.transport.public.youbike.title}</h3>
							<p class="transport-section-desc">{info.transport.public.youbike.description}</p>
							<div class="transport-stations">
								{info.transport.public.youbike.stations.map((station: string) => <span class="transport-station-tag">{station}</span>)}
							</div>
						</div>

						<div class="transport-section">
							<h3 class="transport-section-title">{info.transport.public.bus.title}</h3>
							{
								info.transport.public.bus.routes.map((route: { from: string; description: string; to: string; buses: string[] }) => (
									<div class="transport-card">
										<div class="transport-route">
											<div class="transport-route-left">{route.from}</div>
											<div class="transport-route-line" aria-hidden="true" />
											<div class="transport-route-right">{route.to}</div>
										</div>
										<p class="transport-route-desc">{route.description}</p>
										<div class="transport-buses">
											{route.buses.map((bus: string) => (
												<span class="transport-bus-tag">{bus}</span>
											))}
										</div>
									</div>
								))
							}
						</div>
					</section>

					<section class="transport-panel" data-transport-panel="drive" role="tabpanel" hidden>
						<div class="transport-card">
							<h3 class="transport-card-title">{info.transport.drive.car.title}</h3>
							<p set:html={info.transport.drive.car.description} />
						</div>

						<div class="transport-card">
							<h3 class="transport-card-title">{info.transport.drive.motorcycle.title}</h3>
							<p set:html={info.transport.drive.motorcycle.description} />
						</div>

						<div class="transport-references">
							<h4 class="transport-references-title">{info.transport.drive.referencesTitle}</h4>
							<ul class="transport-references-list">
								{
									info.transport.drive.references.map((ref: { name: string; url: string }) => (
										<li>
											<a href={ref.url} target="_blank" rel="noopener noreferrer">
												{ref.name}
											</a>
										</li>
									))
								}
							</ul>
						</div>
					</section>
				</div>
			</div>
		</div>
	</dialog>

	<Footer />
</Layout>

<script>
	const initInfoPage = () => {
		const dialog = document.querySelector<HTMLDialogElement>("[data-transport-dialog]");
		const openButton = document.querySelector<HTMLButtonElement>("[data-open-transport]");
		const closeButton = document.querySelector<HTMLButtonElement>("[data-close-transport]");
		const intoBtn = document.getElementById("into-academia-sinica");
		if (!dialog || !openButton || !closeButton || !intoBtn) return () => {};
		if (dialog.dataset.infoInit === "true") return () => {};
		dialog.dataset.infoInit = "true";

		const tabs = Array.from(document.querySelectorAll<HTMLButtonElement>("[data-transport-tab]"));
		const panels = Array.from(document.querySelectorAll<HTMLElement>("[data-transport-panel]"));
		const tabOrder = ["shuttle", "public", "drive"];
		const reduceMotionQuery = window.matchMedia("(prefers-reduced-motion: reduce)");
		let redirectTimer: number | null = null;
		let panelAnimationTimer: number | null = null;
		let activeTransportTab = panels.find(panel => !panel.hidden)?.getAttribute("data-transport-panel") ?? "shuttle";
		let exiting = false;

		const setModalScrollLock = (locked: boolean) => {
			document.body.style.overflow = locked ? "hidden" : "";

			const lenis = (window as any).lenis;
			if (locked && lenis && typeof lenis.stop === "function") {
				lenis.stop();
			}
			if (!locked && lenis && typeof lenis.start === "function") {
				lenis.start();
			}
		};

		const clearPanelMotionClasses = (panel: HTMLElement) => {
			panel.classList.remove("is-entering", "is-active-transition", "is-exiting-left", "is-exiting-right", "from-left", "from-right");
		};

		const settlePanels = (name: string) => {
			panels.forEach(panel => {
				const active = panel.getAttribute("data-transport-panel") === name;
				clearPanelMotionClasses(panel);
				panel.toggleAttribute("hidden", !active);
				panel.setAttribute("aria-hidden", active ? "false" : "true");
			});
		};

		const activateTab = (name: string, options: { immediate?: boolean } = {}) => {
			if (!tabOrder.includes(name)) return;
			if (name === activeTransportTab && !options.immediate) return;

			if (panelAnimationTimer !== null) {
				window.clearTimeout(panelAnimationTimer);
				panelAnimationTimer = null;
			}

			const previousName = activeTransportTab;
			const previousPanel = panels.find(panel => panel.getAttribute("data-transport-panel") === previousName);
			const nextPanel = panels.find(panel => panel.getAttribute("data-transport-panel") === name);
			const direction = tabOrder.indexOf(name) >= tabOrder.indexOf(previousName) ? 1 : -1;

			tabs.forEach(btn => {
				const active = btn.getAttribute("data-transport-tab") === name;
				btn.classList.toggle("is-active", active);
				btn.setAttribute("aria-selected", active ? "true" : "false");
			});

			if (options.immediate || reduceMotionQuery.matches || !previousPanel || !nextPanel) {
				settlePanels(name);
				nextPanel?.scrollTo({ top: 0 });
				activeTransportTab = name;
				return;
			}

			panels.forEach(panel => {
				const panelName = panel.getAttribute("data-transport-panel");
				const relevant = panelName === previousName || panelName === name;
				clearPanelMotionClasses(panel);
				panel.toggleAttribute("hidden", !relevant);
				panel.setAttribute("aria-hidden", panelName === name ? "false" : "true");
			});

			previousPanel.classList.add(direction > 0 ? "is-exiting-left" : "is-exiting-right");
			nextPanel.classList.add("is-entering", direction > 0 ? "from-right" : "from-left");
			nextPanel.scrollTo({ top: 0 });

			window.requestAnimationFrame(() => {
				nextPanel.classList.add("is-active-transition");
			});

			activeTransportTab = name;
			panelAnimationTimer = window.setTimeout(() => {
				settlePanels(name);
				panelAnimationTimer = null;
			}, 280);
		};

		const openDialog = () => {
			setModalScrollLock(true);
			if (typeof dialog.showModal === "function") dialog.showModal();
			else dialog.setAttribute("open", "");

			activateTab("shuttle", { immediate: true });
		};

		const closeDialog = () => {
			if (dialog.open) dialog.close();
			else dialog.removeAttribute("open");
		};

		const onDialogClose = () => {
			setModalScrollLock(false);
		};

		const onDialogClick = (e: MouseEvent) => {
			if (e.target === dialog) dialog.close();
		};

		const tabHandlers = tabs.map(btn => {
			const handler = () => {
				const name = btn.getAttribute("data-transport-tab");
				if (name) activateTab(name);
			};
			btn.addEventListener("click", handler);
			return [btn, handler] as const;
		});

		const hero = document.querySelector<HTMLElement>("header");
		const heroContent = hero?.querySelector<HTMLElement>(".hero-content");
		const main = document.querySelector<HTMLElement>("main");
		const footer = document.querySelector<HTMLElement>("footer");

		const enterAcademiaSinica = () => {
			if (exiting) return;
			exiting = true;

			const html = document.documentElement;
			html.style.scrollBehavior = "auto";

			document.documentElement.scrollTop = 0;
			document.body.scrollTop = 0;
			window.scrollTo(0, 0);

			const scrollY = window.scrollY;
			document.body.style.position = "fixed";
			document.body.style.top = `-${scrollY}px`;
			document.body.classList.add("page-lock");

			if (hero) {
				const rect = hero.getBoundingClientRect();
				hero.style.height = `${rect.height}px`;
				hero.style.paddingTop = getComputedStyle(hero).paddingTop;
				hero.style.paddingBottom = getComputedStyle(hero).paddingBottom;
			}

			heroContent?.classList.add("fade-out");
			main?.classList.add("fade-out");
			footer?.classList.add("fade-out");
			intoBtn.classList.add("fade-out");

			if (hero) hero.style.height = "100vh";
			if (hero) hero.style.width = "100vw";

			const href = intoBtn.dataset.academiaSinicaHref ?? "/2026/info/academia-sinica/";

			redirectTimer = window.setTimeout(() => {
				window.location.href = href;
			}, 300);
		};

		openButton.addEventListener("click", openDialog);
		closeButton.addEventListener("click", closeDialog);
		dialog.addEventListener("close", onDialogClose);
		dialog.addEventListener("click", onDialogClick);
		intoBtn.addEventListener("click", enterAcademiaSinica);

		return () => {
			if (redirectTimer !== null) window.clearTimeout(redirectTimer);
			if (panelAnimationTimer !== null) window.clearTimeout(panelAnimationTimer);
			setModalScrollLock(false);
			openButton.removeEventListener("click", openDialog);
			closeButton.removeEventListener("click", closeDialog);
			dialog.removeEventListener("close", onDialogClose);
			dialog.removeEventListener("click", onDialogClick);
			intoBtn.removeEventListener("click", enterAcademiaSinica);
			tabHandlers.forEach(([btn, handler]) => btn.removeEventListener("click", handler));
			document.body.classList.remove("page-lock");
			document.body.style.position = "";
			document.body.style.top = "";
			document.documentElement.style.scrollBehavior = "";
			dialog.dataset.infoInit = "false";
		};
	};

	let cleanup: (() => void) | null = null;
	const mount = () => {
		cleanup?.();
		cleanup = initInfoPage();
	};
	const unmount = () => {
		cleanup?.();
		cleanup = null;
	};

	if (document.readyState === "loading") {
		document.addEventListener("DOMContentLoaded", mount, { once: true });
	} else {
		mount();
	}

	document.addEventListener("astro:page-load", mount);
	document.addEventListener("astro:before-swap", unmount);
</script>

<style>
	header {
		position: relative;
		overflow: hidden;
		display: flex;
		align-items: center;
		justify-content: center;
		padding-block: 13rem;
		background: var(--metal-bg);
	}

	.hero-content {
		position: relative;
		display: flex;
		flex-direction: column;
		gap: 2.69rem;
		z-index: 2;
		align-items: center;
		text-align: center;
	}

	.hero-bg {
		position: absolute;
		inset: 0;
		z-index: 0;
		pointer-events: none;
	}

	.hero-bg-img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	.hero-button {
		padding: 1rem 2rem;
		font-size: 1.25rem;
		font-weight: bold;
		background: linear-gradient(145deg, var(--metal-cream), var(--metal-gold));
		color: var(--metal-ink);
		border-radius: 10rem;
		border: 1px solid rgba(242, 216, 148, 0.5);
		cursor: pointer;
		box-shadow: var(--metal-card-shadow);
	}

	.hero-into-button {
		padding: 1rem 2rem;
		font-size: 1rem;
		font-weight: bold;
		background: rgba(255, 244, 214, 0.14);
		color: var(--metal-cream);
		border-radius: 10rem;
		border: 1px solid rgba(242, 216, 148, 0.32);
		cursor: pointer;
		position: absolute;
		bottom: 1.63rem;
		right: 2.19rem;
		z-index: 2;
		opacity: 80%;
	}

	main {
		background: radial-gradient(circle at 12% 8%, rgba(242, 216, 148, 0.12), transparent 26rem), linear-gradient(180deg, rgba(244, 239, 229, 0.96), rgba(229, 218, 195, 0.94));
		color: var(--coffee);
		padding: clamp(3.5rem, 7vw, 6rem) 2rem;
	}

	.activities-container {
		max-width: var(--content-max-width);
		margin-inline: auto;
	}

	.souvenirs-container {
		max-width: var(--content-max-width);
		margin-inline: auto;
		padding-top: 3rem;
		border-top: 1px solid rgba(45, 33, 17, 0.22);
		margin-top: 2rem;
	}

	.souvenirs-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
		gap: 2rem;
		padding: 1rem 2rem 2rem;
	}

	.souvenir-card {
		border-radius: 0.5rem;
		overflow: hidden;
		border: 1px solid rgba(45, 33, 17, 0.22);
		display: flex;
		flex-direction: column;
		background: rgba(255, 255, 255, 0.5);
		box-shadow: 0 1rem 2rem rgba(45, 33, 17, 0.12);
	}

	.souvenir-image {
		width: 100%;
		aspect-ratio: 1 / 1;
		object-fit: cover;
		display: block;
		color: transparent;
		background-color: rgba(0, 0, 0, 0.05);
	}

	.souvenir-info {
		padding: 1rem 1.25rem 1.25rem;
		display: flex;
		flex-direction: column;
		gap: 0.375rem;
	}

	.souvenir-name {
		font-size: 1.25rem;
		margin: 0;
	}

	.souvenir-price {
		font-size: 1rem;
		font-weight: bold;
		margin: 0;
		opacity: 0.7;
	}

	.activities-section {
		border-top: 1px solid rgba(45, 33, 17, 0.22);
		padding: 3rem 0rem;
	}

	h2 {
		font-size: clamp(2.5rem, 6vw, 4rem);
		margin: 2rem 0;
	}

	h3 {
		font-size: clamp(2rem, 5vw, 3rem);
		margin: 1rem 0 2.5rem;
	}

	h4 {
		font-size: 2rem;
		margin-bottom: 1rem;
	}

	.activity {
		margin: 2.5rem 0;
		gap: 2.75rem;
		display: flex;
		align-items: center;
	}

	.activity:nth-child(odd) {
		flex-direction: row-reverse;
	}

	.activity img {
		width: 18.75rem;
		height: 18.75rem;
		max-width: 100%;
		flex-shrink: 0;
		object-fit: cover;
		border-radius: 0.5rem;
		box-shadow: 0 1rem 2rem rgba(45, 33, 17, 0.14);
	}

	:global(table) {
		display: table;
		width: 100%;
		border-collapse: separate;
		border-spacing: 0;
		margin: 1.5rem 0;
		background: #fff;
		border-radius: 0.75rem;
		overflow: hidden;
		border: 2px solid rgba(180, 148, 76, 0.2);
	}

	:global(table th),
	:global(table td) {
		padding: 1rem 1.25rem;
		text-align: left;
		border-bottom: 1px solid rgba(180, 148, 76, 0.15);
	}

	:global(table th) {
		background: rgba(180, 148, 76, 0.15);
		font-weight: bold;
		font-size: 0.95rem;
		text-transform: uppercase;
		letter-spacing: 0.03em;
		color: #6b5528;
		border-bottom: 2px solid rgba(180, 148, 76, 0.3);
	}

	:global(table tbody td) {
		transition:
			background 0.2s ease,
			color 0.2s ease;
	}

	:global(table tbody tr:hover td),
	:global(table tbody:hover td[rowspan]) {
		background: rgba(180, 148, 76, 0.06);
	}

	:global(table tbody tr:last-child td) {
		border-bottom: none;
	}

	:global(table td) {
		font-size: 0.9375rem;
		color: var(--coffee);
	}

	:global(table tr td:first-child) {
		font-weight: 500;
	}

	.modal {
		display: none;
		border: none;
		padding: 0;
		background: transparent;
		color: var(--silence-white);

		width: 100%;
		max-width: none;
		height: 100%;
		max-height: none;
		position: fixed;
	}

	.modal[open] {
		display: grid;
		place-items: center;
	}
	:root:has(dialog[open]),
	body:has(dialog[open]) {
		height: 100vh;
		overflow: hidden;
	}

	.modal::backdrop {
		background: rgba(0, 0, 0, 0.45);
		backdrop-filter: blur(2px);
	}

	.modal-card {
		width: min(50rem, calc(100vw - 2rem));
		padding: 4rem;
		border-radius: 1rem;
		background: rgba(0, 0, 0, 0.44);
		margin: 1rem 0;
	}

	.modal-header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 1.25rem 1.25rem 0.75rem;
	}

	.modal-button {
		background: linear-gradient(90deg, #edb73d 0%, #d1a643 25%, #c39d47 50%, #c09b48 75%, #b6944acc 100%);
		border: 1px solid var(--copper);
		border-radius: 0.75rem;
		cursor: pointer;
		font-size: 1.125rem;
		padding: 0.625rem 1.75rem;
		color: #12121280;
		font-weight: bold;
	}

	.modal-button.is-active {
		filter: brightness(1.05);
		outline: 2px solid rgba(255, 255, 255, 0.35);
		outline-offset: 2px;
	}

	.modal-button-container {
		display: flex;
		gap: 1rem;
		align-items: center; /* horizontal center for column flex */
		justify-content: space-between;
	}

	.modal-title {
		font-size: 2rem;
		margin: 0;
	}

	.modal-x {
		border: none;
		background: rgb(0, 0, 0);
		color: var(--silence-white);

		width: 2.25rem;
		height: 2.25rem;
		border-radius: 999px;

		display: grid;
		place-items: center;

		cursor: pointer;
		padding: 0;
		line-height: 0;
	}

	.modal-x:hover {
		background: rgba(255, 255, 255, 0.1);
	}

	.modal-body {
		padding: 1rem 1.25rem 1.25rem;
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
	}

	.transport-panel {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.transport-panel[hidden] {
		display: none !important;
	}

	.transport-route {
		display: flex;
		align-items: center;
		flex-wrap: wrap;
		gap: 1.25rem;
		color: var(--silence-white);
		font-weight: 500;
		font-size: 1.25rem;
	}

	.transport-route-left,
	.transport-route-right {
		white-space: nowrap;
	}

	.transport-route-line {
		position: relative;
		flex: 1;
		height: 2px;
		background: var(--silence-white);
		border-radius: 999px;
		min-width: 3rem;
	}

	.transport-route-line::after {
		content: "";
		position: absolute;
		right: 0;
		top: 50%;
		width: 10px;
		height: 10px;
		border-right: 2px solid var(--silence-white);
		border-top: 2px solid var(--silence-white);
		transform: translateY(-50%) rotate(45deg);
	}

	.transport-card {
		border-radius: 0.5rem;
		padding: 1.25rem 1.5rem;
		background: rgba(0, 0, 0, 0.25);
		border: 1px solid rgba(255, 255, 255, 0.3);
	}

	.transport-card p {
		margin: 0;
		font-size: 1rem;
		color: var(--silence-white);
		font-weight: 500;
	}

	.transport-card :global(a) {
		color: #edb73d;
		text-decoration: underline;
	}

	.transport-card-title {
		font-size: 1.125rem;
		font-weight: bold;
		color: var(--silence-white);
		margin: 0 0 0.75rem 0;
	}

	.transport-section {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.transport-section-title {
		font-size: 1.25rem;
		font-weight: bold;
		color: var(--silence-white);
		margin: 0;
	}

	.transport-section-desc {
		font-size: 0.9375rem;
		color: rgba(255, 255, 255, 0.8);
		margin: 0;
	}

	.transport-stations {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
	}

	.transport-station-tag {
		background: rgba(255, 255, 255, 0.15);
		border: 1px solid rgba(255, 255, 255, 0.3);
		border-radius: 0.5rem;
		padding: 0.375rem 0.75rem;
		font-size: 0.875rem;
		color: var(--silence-white);
	}

	.transport-route-desc {
		font-size: 0.9375rem;
		color: rgba(255, 255, 255, 0.8);
		margin: 0.75rem 0;
	}

	.transport-buses {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
		margin-top: 0.5rem;
	}

	.transport-bus-tag {
		background: linear-gradient(90deg, #edb73d 0%, #c09b48 100%);
		border-radius: 0.375rem;
		padding: 0.25rem 0.625rem;
		font-size: 0.875rem;
		color: #121212;
		font-weight: 600;
	}

	.transport-references {
		margin-top: 0.5rem;
	}

	.transport-references-title {
		font-size: 1rem;
		font-weight: bold;
		color: var(--silence-white);
		margin: 0 0 0.5rem 0;
	}

	.transport-references-list {
		margin: 0;
		padding-left: 1.25rem;
	}

	.transport-references-list li {
		font-size: 0.9375rem;
		color: var(--silence-white);
		margin-bottom: 0.25rem;
	}

	.transport-references-list a {
		color: #edb73d;
		text-decoration: underline;
	}

	@media (max-width: 880px) {
		.hero-button {
			opacity: 80%;
		}

		header {
			flex-direction: column;
			gap: 1rem;
			padding-block: 10rem 3rem;
		}

		.hero-into-button {
			position: static;
			bottom: auto;
			right: auto;

			margin-top: 0.5rem;
		}

		.activity {
			display: block;
			margin: 0 0 3rem 0;
			padding: 0;
		}

		h3 {
			margin-inline: 0;
		}

		.activity img {
			width: 100%;
			height: auto;
			max-height: 20rem;
			aspect-ratio: 35 / 20;
			object-fit: cover;
			border-radius: 0.75rem;
			margin-bottom: 2rem;
		}

		.activities-section {
			padding: 1.5rem 0rem;
		}
	}

	/* For modal */
	@media (max-width: 730px) {
		.modal-card {
			padding: 0.5rem;
		}
		.transport-route-left,
		.transport-route-right {
			white-space: normal;
		}
		.modal-button-container {
			flex-direction: column;
			gap: 0.75rem;
		}

		.modal-button {
			width: 100%;
		}
	}

	.page-lock {
		overflow: hidden;
	}

	.hero {
		transition:
			height 700ms ease,
			padding-top 700ms ease,
			padding-bottom 700ms ease;
		will-change: height, padding-top, padding-bottom;
	}

	.fade-out {
		opacity: 0;
		transform: translateY(10px);
		transition:
			opacity 700ms ease,
			transform 700ms ease;
		pointer-events: none;
	}

	.hero-content,
	.activities,
	footer,
	.hero-into-button {
		transition:
			opacity 700ms ease,
			transform 700ms ease;
	}

	.hero-bg :global(img.hero-bg-img) {
		transition: transform 700ms ease;
		will-change: transform;
	}

	@media (prefers-reduced-motion: reduce) {
		.hero {
			transition: none;
		}

		.fade-out {
			transition: none;
		}

		.hero-content,
		.activities,
		footer,
		.hero-into-button {
			transition: none;
		}

		.hero-bg :global(img.hero-bg-img) {
			transition: none;
		}

		:global(table tbody td) {
			transition: none;
		}

		.modal[open],
		.modal[open]::backdrop,
		.modal[open] .modal-card {
			animation: none;
		}
	}

	.modal[open] {
		padding: 1.25rem;
		animation: transport-modal-in 0.22s ease-out both;
	}

	.modal::backdrop {
		background: rgba(10, 9, 7, 0.72);
		backdrop-filter: blur(0.75rem);
		-webkit-backdrop-filter: blur(0.75rem);
	}

	.modal[open]::backdrop {
		animation: transport-modal-backdrop-in 0.22s ease-out both;
	}

	.modal-card {
		width: min(56rem, calc(100vw - 2rem));
		height: min(44rem, calc(100svh - 2rem));
		max-height: calc(100svh - 2rem);
		display: flex;
		flex-direction: column;
		overflow: hidden;
		overscroll-behavior: contain;
		padding: clamp(1rem, 3vw, 2rem);
		border-radius: var(--metal-radius);
		border: 1px solid rgba(242, 216, 148, 0.34);
		background: radial-gradient(circle at 14% 8%, rgba(242, 216, 148, 0.18), transparent 22rem), linear-gradient(145deg, rgba(28, 25, 20, 0.94), rgba(12, 11, 9, 0.94));
		box-shadow: 0 1.5rem 4rem rgba(0, 0, 0, 0.44);
		color: var(--metal-cream);
	}

	.modal[open] .modal-card {
		animation: transport-modal-card-in 0.32s cubic-bezier(0.16, 1, 0.3, 1) both;
	}

	@keyframes transport-modal-in {
		from {
			opacity: 0;
		}

		to {
			opacity: 1;
		}
	}

	@keyframes transport-modal-backdrop-in {
		from {
			background: rgba(10, 9, 7, 0);
			backdrop-filter: blur(0);
			-webkit-backdrop-filter: blur(0);
		}

		to {
			background: rgba(10, 9, 7, 0.72);
			backdrop-filter: blur(0.75rem);
			-webkit-backdrop-filter: blur(0.75rem);
		}
	}

	@keyframes transport-modal-card-in {
		from {
			opacity: 0;
			transform: translateY(1.5rem) scale(0.985);
		}

		to {
			opacity: 1;
			transform: translateY(0) scale(1);
		}
	}

	.modal-header {
		padding: 0 0 1.2rem;
		border-bottom: 1px solid rgba(242, 216, 148, 0.18);
	}

	.modal-title {
		font-size: clamp(1.8rem, 4vw, 3rem);
		color: var(--metal-cream);
	}

	.modal-x {
		width: 2.5rem;
		height: 2.5rem;
		border: 1px solid rgba(242, 216, 148, 0.28);
		background: rgba(255, 244, 214, 0.08);
		color: var(--metal-cream);
		transition:
			background 0.2s ease,
			border-color 0.2s ease,
			transform 0.2s ease;
	}

	.modal-x:hover {
		background: rgba(255, 244, 214, 0.16);
		border-color: rgba(242, 216, 148, 0.48);
		transform: rotate(90deg);
	}

	.modal-body {
		flex: 1;
		min-height: 0;
		padding: 1.35rem 0 0;
		display: grid;
		grid-template-rows: auto minmax(0, 1fr);
		gap: 1rem;
		overflow: hidden;
	}

	.modal-button-container {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 0.65rem;
		padding: 0.35rem;
		border: 1px solid rgba(242, 216, 148, 0.18);
		border-radius: var(--metal-radius);
		background: rgba(255, 244, 214, 0.06);
	}

	.modal-button {
		min-height: 3rem;
		padding: 0.65rem 1rem;
		border-radius: calc(var(--metal-radius) - 0.125rem);
		border: 1px solid transparent;
		background: transparent;
		color: rgba(244, 239, 229, 0.68);
		font-size: 1rem;
		transition:
			background 0.2s ease,
			color 0.2s ease,
			border-color 0.2s ease;
	}

	.modal-button:hover,
	.modal-button.is-active {
		background: linear-gradient(145deg, rgba(255, 246, 220, 0.18), rgba(141, 99, 48, 0.22));
		border-color: rgba(242, 216, 148, 0.26);
		color: var(--metal-cream);
	}

	.modal-button.is-active {
		outline: none;
	}

	.transport-panel-viewport {
		position: relative;
		min-height: 0;
		overflow: hidden;
	}

	.transport-panel {
		height: 100%;
		overflow-y: auto;
		overscroll-behavior: contain;
		-webkit-overflow-scrolling: touch;
		scrollbar-gutter: stable;
		padding: 0 0.35rem 0.15rem 0;
		transition:
			opacity 0.28s ease,
			transform 0.28s ease;
		will-change: opacity, transform;
	}

	.transport-panel::-webkit-scrollbar {
		width: 0.45rem;
	}

	.transport-panel::-webkit-scrollbar-track {
		background: rgba(255, 244, 214, 0.08);
		border-radius: 999px;
	}

	.transport-panel::-webkit-scrollbar-thumb {
		background: rgba(242, 216, 148, 0.42);
		border-radius: 999px;
	}

	.transport-panel.is-entering.from-right {
		opacity: 0;
		transform: translateX(1.6rem);
	}

	.transport-panel.is-entering.from-left {
		opacity: 0;
		transform: translateX(-1.6rem);
	}

	.transport-panel.is-entering.is-active-transition {
		opacity: 1;
		transform: translateX(0);
	}

	.transport-panel.is-exiting-left,
	.transport-panel.is-exiting-right {
		position: absolute;
		inset: 0;
		pointer-events: none;
		overflow: hidden;
	}

	.transport-panel.is-exiting-left {
		opacity: 0;
		transform: translateX(-1.6rem);
	}

	.transport-panel.is-exiting-right {
		opacity: 0;
		transform: translateX(1.6rem);
	}

	.transport-route {
		color: var(--metal-cream);
		gap: 1rem;
	}

	.transport-route-line {
		background: linear-gradient(90deg, rgba(242, 216, 148, 0.18), var(--metal-gold-soft));
	}

	.transport-route-line::after {
		border-color: var(--metal-gold-soft);
	}

	.transport-card,
	.transport-section {
		border: 1px solid rgba(242, 216, 148, 0.18);
		border-radius: var(--metal-radius);
		background: rgba(255, 244, 214, 0.07);
	}

	.transport-section {
		padding: 1.2rem;
	}

	.transport-card {
		padding: 1.2rem;
	}

	.transport-card p,
	.transport-section-desc,
	.transport-route-desc,
	.transport-references-list li {
		color: rgba(244, 239, 229, 0.78);
	}

	.transport-card-title,
	.transport-section-title,
	.transport-references-title {
		color: var(--metal-cream);
	}

	.transport-station-tag,
	.transport-bus-tag {
		border: 1px solid rgba(242, 216, 148, 0.26);
		border-radius: 999px;
		background: rgba(255, 244, 214, 0.1);
		color: var(--metal-cream);
	}

	.transport-card :global(a),
	.transport-references-list a {
		color: var(--metal-gold-soft);
		text-underline-offset: 0.18em;
	}

	@media (max-width: 730px) {
		.modal[open] {
			padding: 0.75rem;
			align-items: end;
		}

		.modal-card {
			width: 100%;
			height: min(42rem, calc(100svh - 1.5rem));
			max-height: calc(100svh - 1.5rem);
			padding: 1rem;
		}

		.modal-button-container {
			grid-template-columns: 1fr;
		}
	}
</style>
